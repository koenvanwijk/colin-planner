[project]
name = "colin-planner"
version = "0.1.0"
description = "Student planning assistant"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
]

[tool.uvicorn]
app = "src.app:app"
host = "0.0.0.0"
port = 8080
