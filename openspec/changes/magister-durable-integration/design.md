# Design

- Introduce a Magister adapter module in the planner repo
- Support both:
  1) Official export (if available)
  2) Unofficial API (token-based) with configurable tenant
- Normalize outputs to a single schema for planner
