# Tasks

1. Add missing endpoints to `src/app.py` (magister, voetbal, ical export)
2. Add/align Pydantic models with OpenAPI schemas
3. Ensure correct content-type for iCal endpoint
4. Quick manual test (curl) / health check
